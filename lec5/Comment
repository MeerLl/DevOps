В этой in-memory настройке обе задачи CPU-bound из-за операций с regex. 
Multiprocessing выигрывает (быстрее на ~15 - 20% при большом объеме данных), т к использует отдельные процессы, обходя GIL и позволяя "настоящую параллельность" для CPU-bound задач.
Threading ограничен GIL для CPU-bound, поэтому медленнее, но лучше подходит для I/O-bound (например, реальное чтение файлов с диска), где GIL не мешает, а потоки эфективно ждут I/O.
