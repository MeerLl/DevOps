[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "docker-telegram-bot"
version = "0.1.0"
description = "Async Telegram bot for Docker monitoring and autoscaling"
requires-python = ">=3.10"

[tool.pytest.ini_options]
asyncio_mode = "auto"

